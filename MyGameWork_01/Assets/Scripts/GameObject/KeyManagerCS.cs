using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KeyManagerCS : MonoBehaviour
{
    private bool keyFlag;

    public bool KeyFlag
	{
		get { return keyFlag; }
		set { keyFlag = value; }
	}

	// Start is called before the first frame update
	void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
